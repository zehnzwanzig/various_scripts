Takes a screenshot of a specified website every day at 12PM by using ScreenshotMachine.

Setup.bat creates the directorys, moves the files in there and schedules the task on your windows machine.
Start.bat is used by the task to run the python script.